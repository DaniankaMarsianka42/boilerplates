# Генерация в гошный код

Taskfile не позволяет сгенерировать в гошный код(скорее всего из-за того, что тамнеправильно указаны пути)

Для генерации в гошный код нужно из этого директория в терминале ввести следующую команду(перед этим установить `buf.exe`)
- ```..\..\bin\buf.exe generate```   